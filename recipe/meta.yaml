{% set name = "sixelcrop" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sixelcrop-{{ version }}.tar.gz
  sha256: 15a4f3c92e1d8b70e3c41c4dfd06be644cf62c78e777633e6fad4bded5348951

build:
  entry_points:
    - sixelcrop = sixelcrop:cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - hatchling
    - pip
  run:
    - python >=3.7

test:
  imports:
    - sixelcrop
  commands:
    - pip check
    - sixelcrop --help
  requires:
    - pip

about:
  home: https://pypi.org/project/sixelcrop/
  summary: A library for cropping sixel images
  dev_url: https://github.com/joouha/sixelcrop
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - jan-janssen
